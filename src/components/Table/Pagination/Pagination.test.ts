import React from 'react'
import { render } from '@testing-library/react'
import axe from 'axe-core';
import Pagination from './Pagination';

describe("Pagination tests", () => {
    it.todo('should render without accessibility violations', async () => {
    });
    it.todo('should navigate to first page', async () => {
    });
    it.todo('should not navigate to first page when on first page', async () => {
    });
    it.todo('should navigate to next page', async () => {
    });
    it.todo('should not navigate to next page if at end of resultts', async () => {
    });
});